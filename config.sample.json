{
  "_comment": "View src/config.ts for type def",
  "models": [{
    "_comment": "You can add many models here. All of them are loaded at runtime.",
    "id": "1",
    "type": "openAI",
    "config": {
      "apiKey": "YOUR_API_KEY",
      "systemPrompt": ""
    }
  }],
  "_comment_proxy": "Remove 'proxy' if you don't need it",
  "proxy": {
    "host": "HOSTNAME",
    "port": 1080
  }
}